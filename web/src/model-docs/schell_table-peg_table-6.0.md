# Schell Table + Peg Table 6.0

Schell model using the iteration 1 peg table.

- Discard logic: same structure as 5.0, with peg EV sourced from the iteration 1 table.
- Crib logic: uses Schell own/opponent crib tables.
- Pegging logic: uses the iteration 1 peg table for table-covered decisions.

Improvement over prior model: swaps in the newer peg-table recursion output so 5.0 and 6.0 can be compared directly.
