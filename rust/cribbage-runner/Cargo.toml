[package]
name = "cribbage-runner"
version = "0.1.0"
edition = "2021"

[dependencies]
cribbage-shadow-engine = { path = "../cribbage-shadow-engine" }
rusqlite = { version = "0.32", features = ["bundled"] }
