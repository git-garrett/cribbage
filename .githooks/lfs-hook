#!/bin/sh
set -eu

if command -v git-lfs >/dev/null 2>&1; then
  exec git lfs "$@"
fi

if git grep -q 'filter=lfs' -- .gitattributes ':(glob)**/.gitattributes' 2>/dev/null; then
  echo "Git LFS attributes are present but git-lfs is not installed." >&2
  exit 2
fi
